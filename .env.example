# ============================================================
# Story Video Factory — environment configuration
# Copy to .env and fill in your own values.
# ============================================================

# ---------- MiniMax (one key, two services) ----------
MINIMAX_API_KEY=

# LLM
MINIMAX_LLM_URL=https://api.minimax.io/v1
MINIMAX_LLM_MODEL=MiniMax-Text-01
MINIMAX_LLM_TEMPERATURE=0.8

MINIMAX_TTS_URL=https://api.minimax.io/v1/t2a_v2
# TTS
MINIMAX_TTS_MODEL=speech-2.6-hd
# Deep, warm male — best fit for "trầm ấm, nam"
MINIMAX_VOICE_ID=English_causual_narrator_vv1
MINIMAX_DEFAULT_EMOTION=neutral
MINIMAX_DEFAULT_SPEED=0.95

# ---------- Pipeline ----------
# Visual backend: "manual" (default for v1)
GOOGLE_VISUAL_BACKEND=manual
# Default content language
DEFAULT_LANG=vi
# Default aspect ratio
DEFAULT_ASPECT=9:16

PIPELINE_CONCURRENCY=3
OUTPUT_DIR=./outputs
LOG_LEVEL=info

# ---------- Optional (for v2 auto mode) ----------
GOOGLE_GEMINI_API_KEY=
GOOGLE_OMNI_MODEL=veo-2.0-generate-001
